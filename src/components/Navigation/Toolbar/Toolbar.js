import React from 'react';
// import Aux from '../../hoc/Auxillary';

const toolbar = (props) => (
    <header>
        <div>MENU</div>
        <div>LOGO</div>
        <nav>
            ...
        </nav>
    </header>
);

export default toolbar;